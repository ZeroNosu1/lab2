import 'package:flutter/material.dart';

class TwomainPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Twomain Page'),
      ),
      body: Center(
        child: Text('This is the twomain page'),
      ),
    );
  }
}